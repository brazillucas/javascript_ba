let texto = "Exemplo";
let num = 1;
let bool = true;

console.log(`Nomes das variáveis: ${Object.keys({texto})[0]} - ${Object.keys({num})[0]} - ${Object.keys({bool})[0]}`);

console.log(`Valor das variáveis: ${texto} - ${num} - ${bool}`);